# MNIST-Digit-Classifier
A deep learning model built with PyTorch achieving 94%+ accuracy on the MNIST dataset. Uses a 3-layer neural network and custom preprocessing pipeline to recognize handwritten digits from real-world images.
